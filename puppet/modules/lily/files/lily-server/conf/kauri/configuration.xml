<?xml version="1.0"?>
<conf xmlns:conf="http://kauriproject.org/configuration"
      conf:inherit="deep">

  <!-- Lily does not support configuration changes at runtime currently, so makes
       no sense to scan the configuration for changes. -->

  <!-- Interval is in milliseconds -->
  <reloading enabled="false" interval="5000"/>
</conf>